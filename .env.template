# Environment Variables Template
# Copy this file to .env and fill in your values:
#   cp .env.template .env
#
# IMPORTANT: Never commit .env files with actual secrets!

# ===========================================
# Database Configuration (Required)
# ===========================================

# PostgreSQL connection string
# Options:
#   - Local: postgresql://user:password@localhost:5432/eft_tracker
#   - Neon: postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/eft_tracker?sslmode=require
#   - Supabase: postgresql://postgres:password@db.xxx.supabase.co:5432/postgres
#
# Production Notes:
#   - Use Neon pooler endpoint for production (ends with -pooler.*.neon.tech)
#   - Example: postgresql://user:pass@ep-xyz-pooler.us-east-1.aws.neon.tech/neondb
#   - Connection pooling is built-in with the pooler endpoint
DATABASE_URL=op://DevSecrets/Neon/Connection String

# Staging Database (Optional)
# Use Neon database branching to create isolated staging environment
# See docs/DATABASE_BRANCHING.md for setup instructions
# DATABASE_URL_STAGING=postgresql://user:pass@ep-staging-pooler.region.aws.neon.tech/neondb

# ===========================================
# Authentication (Required)
# ===========================================

# NextAuth.js secret - generate with: openssl rand -base64 32
# Production Notes:
#   - MUST be at least 32 characters
#   - MUST be different from development
#   - Rotating this invalidates all active user sessions
#   - Store backup in password manager
AUTH_SECRET=

# App URL (use http://localhost:3000 for local development)
# Production Notes:
#   - MUST match your production domain exactly
#   - Example: https://learntotarkov.com
#   - DO NOT include trailing slash
NEXTAUTH_URL=http://localhost:3000

# ===========================================
# MCP Server Credentials (Optional)
# ===========================================

# GitHub MCP Server - for GitHub integration features
# Get one at: https://github.com/settings/tokens
GITHUB_PERSONAL_ACCESS_TOKEN=

# Brave Search MCP Server - for web search features
# Get one at: https://brave.com/search/api/
BRAVE_API_KEY=

# ===========================================
# Logging Configuration (Optional)
# ===========================================

# Log level for structured logging with Pino
# Options: error, warn, info, debug
# Production Notes:
#   - Use "info" in production (default)
#   - Use "debug" for troubleshooting (verbose)
#   - Logs are JSON in production, pretty-printed in development
LOG_LEVEL=info

# ===========================================
# Error Monitoring with Sentry (Optional)
# ===========================================

# Sentry DSN - Get from https://sentry.io/settings/projects/[project]/keys/
# Production Notes:
#   - Create separate Sentry projects for staging and production
#   - Use different DSNs for each environment
#   - Free tier: 5,000 errors/month (sufficient for most projects)
SENTRY_DSN=

# Public Sentry DSN for client-side error tracking
# Same as SENTRY_DSN but exposed to browser
NEXT_PUBLIC_SENTRY_DSN=

# Sentry organization slug (e.g., "my-company")
# Used for uploading source maps during build
SENTRY_ORG=

# Sentry project slug (e.g., "eft-tracker")
# Used for uploading source maps during build
SENTRY_PROJECT=

# Sentry Auth Token - Get from https://sentry.io/settings/account/api/auth-tokens/
# Required for uploading source maps in production builds
# Scopes needed: project:read, project:releases, org:read
# Production Notes:
#   - Only needed in CI/CD and production builds
#   - Not required for local development
#   - Keep this secret and never commit it
SENTRY_AUTH_TOKEN=
